
SELECT
  bitmapToArray(
    groupBitmapAndState(users)
  )
FROM acc_inv_user_group_1d


insert into t1 select 'tag1', 101;

insert into t1 select 'tag1', 103;


insert into user_tag_value_long

SELECT '3', '3', 't3',

    groupBitmapState(channel_id) channel_ids ,
    '2024-03-11'

  FROM t1
  GROUP BY tag_name
  ;

  SELECT
    bitmapToArray(
      groupBitmapAndState(users)
    )
  FROM user_tag_value_long
  where tag_id = 3