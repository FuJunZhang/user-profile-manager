<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="config.properties"></properties>
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <typeAliases>
        <package name="com.danan.upm.pojo"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
<!--                <property name="driver" value="${ck.jdbc.driver.name}"/>-->
<!--                <property name="url" value="${ck.jdbc.url}"/>-->
                <property name="driver" value="${starrocks.jdbc.driver.name}"/>
                <property name="url" value="${starrocks.jdbc.url}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <package name="com.danan.upm.mapper"/>
    </mappers>
</configuration>